Video used for tracking: 123.mpg

To run the file, run video_To_Image first to generate the image frames.
Then run image_Eye_Tracking.

To see the output of other files:
"img = imread('Output\*.png');"
Where * is replace by 1-# end number of frames generated by the converter.
